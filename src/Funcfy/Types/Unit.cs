namespace Funcfy.Types;

/// <summary>
/// Represents a type that has no value.
/// In functional programming, this is often used to indicate the absence of a meaningful value, similar to `void` in other languages.
/// This makes it particularly useful in functional contexts where a type is expected but no result is needed.
/// </summary>
public struct Unit { }
